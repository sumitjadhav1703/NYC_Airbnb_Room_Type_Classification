{
  "name": "nyc-airbnb-room-type-predictor-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend for the NYC Airbnb room type classifier",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./style.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
